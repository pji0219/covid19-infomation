{
  "name": "server",
  "version": "1.0.0",
  "engines": {
    "node": "14.16.1"
  },
  "description": "corona info API server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd server && node index.js",
    "client": "cd client && yarn start",
    "backend": "cd server && nodemon index.js",
    "dev": "concurrently \"yarn backend\" \"yarn client\""
  },
  "author": "pji",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "concurrently": "^6.2.0",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.7",
    "request": "^2.88.2"
  }
}